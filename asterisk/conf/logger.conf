[general]
dateformat=%F %T.%3q

[logfiles]
; Console goes to the container log. `messages` stays on disk because fail2ban
; watches it for SIP brute force (see README, security section).
console => notice,warning,error
messages => notice,warning,error
