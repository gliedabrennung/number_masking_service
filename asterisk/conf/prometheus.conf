;
; Channel/bridge/endpoint metrics from Asterisk itself, served by the built-in
; HTTP server on http://127.0.0.1:8088/metrics — the same loopback-only socket
; as ARI. Application metrics live separately on the API's /metrics.
;
[general]
enabled = yes
core_metrics_enabled = yes
uri = metrics
