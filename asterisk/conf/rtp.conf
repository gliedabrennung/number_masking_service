[general]
; 200 ports = 100 concurrent calls, well beyond the 20-call target of the
; prototype. Widen for production and open the same range on the firewall.
rtpstart=10000
rtpend=10200
icesupport=no
strictrtp=yes
